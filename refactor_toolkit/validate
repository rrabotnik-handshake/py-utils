#!/bin/bash
# Simple wrapper for the Python validation tool

# Make sure we're in the right directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

# Run the Python validator with all arguments
python3 validate.py "$@"
